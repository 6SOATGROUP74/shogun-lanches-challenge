apiVersion: v1
kind: Pod
metadata:
  name: app-pod
  labels:
    app: app-pod
spec:
  containers:
    - name: app-container
      image: igoryy/fiap-6soat:2
      imagePullPolicy: Always
      ports:
        - containerPort: 8080
      env:
        - configMapRef:
            name: app-configmap